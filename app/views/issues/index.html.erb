<h1 class="div-on-left">Archives</h1>

<div class="row">
  <div class="span5">
    <% position_counter = 0 %>
  <% if position_counter.even? %>
    <div class="div-on-left">
  <% end %>
<% 
  id = 0 
  @issues.each do |x|
    if (id != x.id)
  %>
  <h4><a href="issues/<%= x.id %>">Issue # <%= x.id %> — <%= x['release_date'] %></a></h4>
  <% 
     id = x.id
     position_counter +=1
    end
  %>
<ul>
  <li>
  <%= x.summary %>
  </li>
</ul>
  <% end %>
    <% if position_counter.even? %>
    </div>
    <% end %>
  </div>
  <br />
</div> <!-- end of row -->
